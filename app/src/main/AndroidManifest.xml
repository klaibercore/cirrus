<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- A streaming reply has to keep reading its socket after Cirrus leaves the screen;
         a cached process is frozen within seconds and would lose the rest of the answer. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Keeps the CPU running between packets while the screen is off. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Dictation in the composer. Requested at the moment the microphone is first tapped. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Coarse only, and on purpose: every use of get_location — weather, what is nearby, travel
         time — is answered by the neighbourhood, and the difference between that and the doorstep
         is the difference between a useful tool and a tracking device. Requested when the Location
         switch is turned on. No background location: an agent running at 3am has no business
         asking where somebody is. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Package visibility: without these, Android 11+ hides the services behind them. Speech
         recognition reports that no recogniser exists, and TextToSpeech fails to bind to any
         engine at all — both look like the feature is broken rather than invisible. -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
        <!-- Apps with a launcher icon, for list_installed_apps and open_app. This is the narrow
             form on purpose: QUERY_ALL_PACKAGES would also reveal every app without an icon,
             which is a far more revealing list than "what can I open for you?" needs. -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <!-- The store listing install_app opens, and a browser to fall back on. -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="market" />
        </intent>
    </queries>

    <application
        android:name=".CirrusApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cirrus"
        android:enableOnBackInvokedCallback="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode|density|fontScale"
            android:theme="@style/Theme.Cirrus">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Where Spotify sends the browser back to after a sign-in. The scheme has to match
                 the redirect URI registered on the Spotify application exactly, and Android routes
                 it here as a new intent on the existing task. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="cirrus" android:host="spotify" />
            </intent-filter>

            <!-- Accept shared text from other apps as a new prompt. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>

        <!-- Hilt builds the workers, so WorkManager must not initialise itself first: the
             on-demand initialiser in CirrusApp installs the Hilt worker factory instead. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- Runs only while a turn is in flight and stops itself as soon as one is not. -->
        <service
            android:name=".service.GenerationService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>

</manifest>
